%h1 Events (League months, tournaments, etc.)

/ TODO: write a dropdown select box that lists all the available event types, with a value of their corresponding ID. Build the event as an associated object belonging to that event type.

= simple_form_for @event, :html => {:class => "form-horizontal"} do |f|

  = f.input :server_id, collection: @servers, default: 1
  = f.input :ruleset_id, collection: @rulesets, default: 1
  = f.input :name
  = f.input :event_type, collection: ["League", "Tournament"], default: 1, label: "Event Type"

  = f.simple_fields_for :accounts do |af|
    = af.input :phrase, label: "Define a Tag Phrase"


  .accordion#accordion1
    .accordion-group
      .accordion-heading
        %a.accordion-toggle{:data => {:toggle => "collapse", :parent => "#accordion1"}, :href => "#collapseOne"}
          Create Ruleset (Optional)
      .accordion-body.collapse#collapseOne
        .accordion-inner
          = f.fields_for(@event.ruleset) do |b|

            = render "ruleset_fields", :b => b

  .actions
    = f.submit "Create Event", class: "btn"