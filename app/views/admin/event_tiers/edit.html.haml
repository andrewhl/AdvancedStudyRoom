- @title << 'Edit tier'

= simple_form_for @tier, url: admin_tier_path(@tier), validate: true, html: {class: 'form-horizontal'} do |f|

  = f.input :name
  = f.input :index, label: 'Position'

  .form-actions
    = f.submit 'Save', class: 'btn btn-primary'
    = link_to 'Back', :back, class: 'btn'
