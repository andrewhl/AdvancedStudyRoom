%h1 Edit Tag

= form_for @tag, :html => {:class => "form-horizontal"} do |f|
  .control-group
    %label.control-label
      Phrase
    .controls
      = f.text_field :phrase

  .control-group
    .controls
      = f.submit :submit