%h1 Edit Tag

= form_for @tag, :html => {:class => "form-horizontal"} do |f|
  .control-group
    %label.control-label
      Phrase
    .controls
      = f.text_field :phrase
    %label.control-label
      Move Limit
    .controls
      = f.number_field :node_limit

  .control-group
    .controls
      = f.submit :submit, class: "btn"